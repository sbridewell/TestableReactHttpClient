import WebApiClientBaseMock from '../WebApiClientBaseMock';

// Export the mock class from here in the __mocks__ folder so that we can use
// this mock in the test for ReactHttpClients
export default WebApiClientBaseMock;